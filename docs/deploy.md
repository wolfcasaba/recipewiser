# Deploying Changes

1.  Commit changes:
    ```bash
    git add .
    git commit -m "Added AI-powered meal planner"
    ```
2.  Push to GitHub:
    ```bash
    git push origin main
    ```
3.  Deploy to Vercel:
    ```bash
    vercel
    ```
